# This workflow will install Python dependencies, run tests and lint with a variety of Python versions
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python package

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  SQLALCHEMY_DATABASE_URI : "postgresql+psycopg2://nathan:abc123@localhost:5432/Vapor"
  SQLALCHEMY_DATABASE_URI : "postgresql://vapor_admin:QwWCQUYVceRTTlatAwUamTEjWh4Nbhdu@dpg-cebpv3mn6mprhjquci50-a.ohio-postgres.render.com/vapor"
  APP_SECRET_KEY : os.urandom(12)
  BCRYPT_ROUNDS : 5
  IGDB_CLIENT_ID : 'nnmpkjm8cks3623ypg6d9j68f3b8rq'
  IGDB_CLIENT_SECRET : 'w01q5virehfbuoaguo9lvuduhts7dj'   

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11"]

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        python -m pip install flake8 pytest
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
    - name: Lint with flake8
      run: |
        # stop the build if there are Python syntax errors or undefined names
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    - name: Test with pytest
      run: |
        pytest
